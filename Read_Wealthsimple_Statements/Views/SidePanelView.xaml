<UserControl x:Class="Read_Wealthsimple_Statements.Views.SidePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Read_Wealthsimple_Statements.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Content="Change Directory" x:Name="btnChangeDirectory" Click="btnChangeDirectory_Click" Grid.Row="0"/>

        <ListBox x:Name="lstBoxStatements" ItemsSource="{Binding AvailableStatements}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{Binding DisplayText}" Padding="4,0,0,4" Background="Transparent"  Tag="{Binding DisplayFilePath}" Click="btnChangeDocument_Click" x:Name="btnChangeDocument"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
